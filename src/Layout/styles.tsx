import { makeStyles, createStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme =>
  createStyles({
    root: {
      background: '#bbdef1',
      minHeight: '97vh',
      padding: 10,
    },
  })
);
